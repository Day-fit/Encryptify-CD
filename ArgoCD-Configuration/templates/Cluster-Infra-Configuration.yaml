apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-infra-configuration
  namespace: argocd
spec:
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  project: default
  source:
    path: Cluster-Infra
    repoURL: https://github.com/Day-fit/Encryptify-CD.git
    targetRevision: HEAD
    helm:
      releaseName: cluster-infra
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default